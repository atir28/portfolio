import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-resume',
  imports: [RouterLink],
  templateUrl: './resume.html',
  styleUrl: './resume.css',
})
export class Resume {}
